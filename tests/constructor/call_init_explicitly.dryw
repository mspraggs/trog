// Foo.init(one)
// Foo.init(two)
// Foo instance
// init
// 0
class Foo {
  init(self, arg) {
    print("Foo.init(" + arg + ")");
    self.field = "init";
  }
}
var foo = Foo("one");
foo.field = "field";
var foo2 = foo.init("two");
print(foo2);
print(foo.field);
