// Outer instance
// Outer instance
// Inner instance
// 0
class Outer {
  method(self) {
    print(self);
    fn f() {
      print(self);
      class Inner {
        method(self) {
          print(self);
        }
      }
      Inner().method();
    }
    f();
  }
}
Outer().method();
