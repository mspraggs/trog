// Outer instance
// Outer instance
// Inner instance
// 0
class Outer {
fn method(self) {
    print(self);
    fn f() {
      print(self);
      class Inner {
      fn method(self) {
          print(self);
        }
      }
      Inner().method();
    }
    f();
  }
}
Outer().method();
