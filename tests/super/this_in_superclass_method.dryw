// a
// b
// 0
class Base {
fn init(self, a) {
    self.a = a;
  }
}
class Derived < Base {
fn init(self, a, b) {
    super.init(a);
    self.b = b;
  }
}
var derived = Derived("a", "b");
print(derived.a);
print(derived.b);
